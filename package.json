{
  "name": "drugwars",
  "version": "1.0.0",
  "description": "Classic Drug Wars text game with a web UI",
  "type": "module",
  "scripts": {
    "dev": "npx live-server --port=3000 --open=/index.html ."
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
